/** Time-to-live for invitation tokens (milliseconds). */
export const INVITE_TTL_MS = 1000 * 60 * 60 * 24 * 7; // 7 days

/** Time-to-live for invitation tokens (seconds). */
export const INVITE_TTL_SECONDS = 60 * 60 * 24 * 7; // 7 days
